---
'@taylorvance/tv-shared-ui': minor
---

Remove raw asset URL helpers from the package root export and expose them from the explicit `./assets` subpath. Add an explicit `./BrandBadge` subpath export for consumers that want a component-only entry.
