name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    uses: ./.github/workflows/ci-verify.yml
    with:
      node-version: '20'
      working-directory: .
      install-command: npm ci
      build-command: npm run build
