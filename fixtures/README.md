# Fixtures

These are internal consumer fixtures for `tv-shared`.

They are not meant to be published. They exist to keep the shared UI honest across the two styling modes that matter right now:
- plain CSS ownership
- utility-class ownership

The fixtures are exercised by tests rather than full app builds so they stay small and fast.
