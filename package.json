{
  "name": "tv-shared",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @tv-shared/ui",
    "build:ui": "npm run build --workspace @tv-shared/ui"
  },
  "devDependencies": {
    "@types/react": "^19.2.5",
    "react": "^19.2.0",
    "typescript": "^5.9.3"
  }
}
