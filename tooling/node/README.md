# Node Tooling

This directory is the source-of-truth home for reusable Node and TypeScript tooling baselines.

Current contents:

- `examples/`: copyable consumer examples for ESLint, Prettier, and TypeScript
